import { createAction, props } from '@ngrx/store';

export const TODO = createAction('[City Component] Exit City');
// export const enterCity = createAction('[City Component] Enter City');

// export const setCurrentPrices = createAction(
//   '[City Component] set Current Prices',
//   props<{ prices: number[] }>()
// );
